# npm start → .env.development (ưu tiên hơn file này)
# docker build / CI → build-arg REACT_APP_* (xem docker-compose.yml)
# Không commit Front-end/.env — dùng .env.development hoặc .env.production

REACT_APP_BACKEND_URL=/e-master
REACT_APP_GOOGLE_CLIENT_ID=
