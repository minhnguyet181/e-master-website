# Local: npm start → .env.development (REACT_APP_BACKEND_URL=/e-master + proxy)
# Docker/prod build → .env.production (REACT_APP_BACKEND_URL=/e-master + nginx)
# Gọi API trực tiếp không qua proxy: REACT_APP_BACKEND_URL=http://localhost:1818/e-master
REACT_APP_GOOGLE_CLIENT_ID=your_google_client_id
