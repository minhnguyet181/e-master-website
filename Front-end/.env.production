# Production — same-origin qua nginx (Front-end/nginx.conf proxy /e-master/)
# Trình duyệt gọi /e-master/* trên https://e-master.id.vn, không cần CORS.
REACT_APP_BACKEND_URL=/e-master
REACT_APP_GOOGLE_CLIENT_ID=
